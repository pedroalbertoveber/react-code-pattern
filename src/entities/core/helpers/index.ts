import { interceptor } from "./interceptor";
import { mergeConfigurations } from "./merge-configurations";
import { makeUrl } from "./make-url";
import { verifyIfIsCustomRequest } from "./verify-if-is-custom-request";

export {
  interceptor,
  mergeConfigurations,
  makeUrl,
  verifyIfIsCustomRequest
}